import 'attackers.dart';
import 'goalkeepers.dart';
import 'player.dart';
import 'players_list.dart';

void main() {
  players.add(p1);
  players.add(p2);
  players.add(p3);

  players.forEach((player) =>
      print(player.name + " " + player.surname + " " + player.getPosition));
  // players.goalPerGame();
while(players == Attacker) {
  print(goalPerGame());
}
